# I'd rather keep patched suckless programs in separate repos
savedconfig

# can be regenerated by portage
zz-autounmask

# generated during install
ufw/ufw/applications.d
portage/portage/conf-update.d

# way too much default crap I don't care to track
libvirt

# installed by installkernel = don't care
kernel/kernel/preinst.d

